# Publication notes

Release model:

- `v1.0.0`: Windows-parity baseline.
- `v2.0.0`: Linux-enhanced edition.

Both versions belong in the same GitHub repository so users can compare history, tags, release notes, and implementation evolution without fragmenting the project.
